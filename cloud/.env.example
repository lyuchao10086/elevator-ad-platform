# cloud/.env.example
# 网关（cloud）示例环境变量

# 当网关收到设备上传的截图或需要回调 control-plane 时，使用此 URL。
# 必须是 control-plane 可达的地址（容器环境下请使用 service 名称），
# 示例：http://control-plane:8000/api/v1/devices/snapshot/callback
CONTROL_PLANE_SNAPSHOT_CALLBACK=http://127.0.0.1:8000/api/v1/devices/snapshot/callback

# 若部署在 k8s，请将上述地址替换为 control-plane service 名称或 ingress 地址
